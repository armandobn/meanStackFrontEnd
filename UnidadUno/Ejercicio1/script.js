// Resolver los siguientes ejercicios:
// 1 - Desplegar todos los dígitos decimales, del 0 al 9, utilizando una estructura de repeticion.
document.write("Desplegar todos los dígitos decimales, del 0 al 9, utilizando una estructura de repeticion.");
for(let i=0; i<10; i++){
  document.write(`<br>${i}`);
}